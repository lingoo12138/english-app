# v1.22.0 Plan - 复习按 tag 过滤 (B12)

**版本号**: v1.22.0
**类型**: W23 候选 — 复习按 tag 过滤
**ROI**: 中
**工期**: 1d
**北极星对齐**: 学得会 (按类别复习) + 触发可业 (自定义复习)

---

## 目标

ReviewCenter 加 tag 过滤, 复用 v1.11 reviewQueue + v1.21 wordTags, 让用户能:
- 按 tag 复习 (只看某类词)
- 不带 tag 的词也能复习 (默认)
- 复习队列按 tag 拆分

---

## 共享约束

### v1.6 review 13 处保护
### v1.7+ listeningRecommend / llmTutor / llm e2e
### v1.8+ Onboarding / aiChat.assessUserLevel / OpenRouter / Daily / WordDetail
### v1.9+ AIChat "✨ 自动" / "💬 自由话题"
### v1.10+ WritePage 中译英 / SynonymsButton / 例句跟读
### v1.11+ FSRS / reviewQueue (核心依赖) / ReportsPage
### v1.12+ llmFallback / imageRecog / llmUsage
### v1.13+ chatRoles 8 角色
### v1.14+ customScenes
### v1.15+ CustomSceneLearn
### v1.16+ sceneReview
### v1.17+ chatRoles 扩展
### v1.18+ fileUpload
### v1.19+ learningCalendar
### v1.20+ notebookBulk
### v1.21+ wordTags (核心依赖)

### 全局
- ✅ 0 P0 + 0 P1 + 0 P2
- ✅ unknown + Error 守卫
- ✅ setLoading 配对
- ✅ useEffect 加依赖
- ❌ 不引付费依赖

---

## 范围

### P1 (主功能): 按 tag 过滤复习
**文件改动**:
- `src/lib/taggedReviews.ts` (~120 行, 新):
  - `getReviewsByTag(tag, onlyDue)`: 按 tag 返 reviews
    - 复用 v1.21 getWordsByTag 拿 wordId
    - 调 v1.11 getAllReviews / getDueReviews
    - 过滤
  - `getReviewCountByTag(tag, onlyDue)`: 计数
  - `getAllTagsWithReviewCount(onlyDue)`: 标签 + 待复习数
  - `getReviewsByTagWithScore(tag, onlyDue)`: 带 score 排序

- `src/pages/ReviewCenter.tsx` (改):
  - 加 tag 过滤 UI (复用 v1.21 模式)
  - 顶部: 全部 / 各 tag + 待复习数
  - 选中 tag 时, 复习队列仅含该 tag 词

### P2 (小优化): UX
- 按 tag 复习完成提示
- 多 tag 组合 (e.g. work + urgent)
- 默认复习不含未带 tag 的词 (选项)

### P3 (验收): 文档
- `docs/CHANGELOG.md` v1.22.0 段
- `docs/RELEASE_v1.22.0.md` 新建
- `README.md` 标题/进度/表格/章节
- `package.json` 1.21.0 → 1.22.0

---

## 测试

- `tests/taggedReviews.test.ts` (~12 测试):
  - getReviewsByTag 返正确 reviews
  - getReviewCountByTag 计数
  - getAllTagsWithReviewCount
  - 边界: 空 tag / 不存在 tag
  - 与 v1.11 reviewQueue 兼容

---

## 验证清单

- [x] `npx tsc --noEmit` 0 错误
- [x] `npx vite build` 成功
- [x] `node scripts/verify-v1.22.0.mjs` 静态 + 单元测试
- [x] `python3 scripts/review-v1.22.0.py` 0 P0 + 0 P1 + 0 P2
- [x] 全部 vitest 全过
- [x] v1.6-v1.21 全部保护

---

## 提交计划

1. `feat(v1.22.0-A): taggedReviews 库 (按 tag 过滤 reviews)`
2. `feat(v1.22.0-B): ReviewCenter 加 tag 过滤 UI`
3. `docs(v1.22.0): CHANGELOG + RELEASE + README + package 1.22.0 + verify/review 脚本`
4. `git tag v1.22.0 + push`

---

**开工!**
