# v1.15.0 Plan - 自定义场景学习流 (B5)

**版本号**: v1.15.0
**类型**: W16 候选 — 完成自定义场景最后一公里
**ROI**: 中
**工期**: 2d → 压缩到 1d (单 producer + 主人接管)
**北极星对齐**: 学得会 (从"看词"到"学词", 闭环 v1.14.0 自定义场景)

---

## 目标

v1.14.0 自定义场景能"看 + 收藏", 但不能"系统学"。本次加 CustomSceneLearn 页面:
- 卡片学习流 (翻页)
- TTS 朗读每个词
- 释义/例句展示
- 收藏 toggle
- 完成进度 + 返回

---

## 共享约束 (不能破坏)

### v1.6 review 13 处保护 (全保)
### v1.7+ listeningRecommend / llmTutor / llm e2e
### v1.8+ Onboarding / aiChat.assessUserLevel / OpenRouter / Daily / WordDetail
### v1.9+ AIChat "✨ 自动" / "💬 自由话题"
### v1.10+ WritePage 中译英 / SynonymsButton / 例句跟读
### v1.11+ FSRS / reviewQueue / ReportsPage
### v1.12+ llmFallback / imageRecog / llmUsage
### v1.13+ chatRoles / RoleSelector / aiChat role
### v1.14+ customScenes / IDB v5 / CustomScenes / CustomSceneDetail

### 全局
- ✅ 0 P0 + 0 P1 + 0 P2
- ✅ unknown + Error 守卫
- ✅ setLoading 配对
- ✅ useEffect 加依赖
- ❌ 不引付费依赖

---

## 范围

### P1 (主功能): CustomSceneLearn 页面
**文件改动**:
- `src/pages/CustomSceneLearn.tsx` (~180 行, 新):
  - URL 参数 `:id` 取场景
  - 状态: `currentIdx` (0-based), `showAnswer` (bool, 默认 false)
  - UI:
    - 顶部进度条 (e.g. "3 / 15")
    - 词卡: 大字 word + 难度标签
    - 点击翻面: 显示 translation + example
    - 底部按钮: ⭐ 收藏 / 🔊 TTS / ← 上一词 / 下一词 →
  - 键盘快捷键: 空格=翻面, ←/→ 翻页
  - 完成: 显示"🎉 学完 N 词!" + 返回详情
  - 路由: `/custom-scenes/:id/learn`

- `src/pages/CustomSceneDetail.tsx` (改):
  - 加 "📚 开始学习" 按钮 (主色, 顶部)
  - 点击 navigate to `/custom-scenes/:id/learn`

- `src/App.tsx` (改):
  - 加路由 `/custom-scenes/:id/learn`

### P2 (小优化): UX
- 进度持久化 (localStorage `customScene-${id}-progress`): 下次继续
- 收藏 toggle 实时 (复用 v1.14 detail 的 isFavorite/addFavorite/removeFavorite)
- TTS 复用现有 `TTSButton` 组件

### P3 (验收): 文档
- `docs/CHANGELOG.md` v1.15.0 段
- `docs/RELEASE_v1.15.0.md` 新建
- `README.md` 标题/进度/表格/章节
- `package.json` 1.14.0 → 1.15.0

---

## 测试

- `tests/customSceneLearn.test.ts` (~8 测试):
  - 计算总词数
  - 进度计算 (currentIdx / total)
  - 是否完成判断 (currentIdx >= total)
  - 收藏 toggle 状态
  - 键盘事件 prev/next
  - 翻面状态切换

(组件层测试覆盖, 不测 LLM/IDB)

---

## 验证清单

- [x] `npx tsc --noEmit` 0 错误
- [x] `npx vite build` 成功
- [x] `node scripts/verify-v1.15.0.mjs` 静态 + 单元测试
- [x] `python3 scripts/review-v1.15.0.py` 0 P0 + 0 P1 + 0 P2
- [x] 全部 vitest 全过
- [x] 路由 /custom-scenes/:id/learn
- [x] Detail 加 "📚 开始学习" 按钮
- [x] TTS 复用
- [x] 收藏 toggle 集成
- [x] 进度持久化

---

## 提交计划

1. `feat(v1.15.0-A): CustomSceneLearn 页面 (卡片流 + TTS + 翻页 + 进度)`
2. `feat(v1.15.0-B): CustomSceneDetail 加 "📚 开始学习" 按钮 + 路由`
3. `docs(v1.15.0): CHANGELOG + RELEASE + README + package 1.15.0 + verify/review 脚本`
4. `git tag v1.15.0 + push`

---

## 风险

- subagent 失败: 主人接管
- TTS 在某些浏览器不支持: 复用 TTSButton 已有处理
- 路由冲突: /custom-scenes/:id 已有, /learn 是子路由

---

**开工!**
