# v1.10.0 — W11 (中译英 + 同义词 + 例句跟读) 3 producer 1d 干完 3d

**日期**: 2026-07-24
**作者**: Mavis
**基线**: v1.8.0 + v1.9.0 (首启 onboarding + 难度自适应)
**目标**: W11 团队推荐 3 件事并行 1d 干完

## 团队讨论产物
- next-phase-plan-v2.md W11 (v1.10.0):
  - **B5 中译英写作** (被砍, 重新提, 留存 3, 成本 2): 给 `/write` 加第二 Tab
  - **B6 同义词辨析 / 混淆词** (留存 3, 成本 2): "happy/glad/joyful", 复用 IndexedDB 缓存
  - **B7 例句 TTS 跟读** (留存 3, 成本 1): 单词详情加 "📢 跟读" 按钮

## 3 个并行 Producer

### P1: v1.10.0-A 中译英 Tab (B5, 2d → 1d 干)
- **Why**: "为什么我的写作批改只能改英文, 不能中译英?" — 学生/职场刚需
- **Scope**:
  - WritePage 加 "中译英" 第二 Tab (现有 Tab 改: ✍️ 批改 | 🌐 中译英)
  - 用户输入中文 → LLM 翻译 + 评分 (A1-C2) + 错误
  - 复用 WritePage 框架 (input/result/history/loading/MAX_LEN 500/日 20 次/写历史)
  - 加 CHINESE_SYSTEM_PROMPT 中译英协议:
    - 协议: `{translation, level, alternatives: [{en, why_better}], notes}`
  - 加 `chineseToEnglish` LLM call
  - 加中文输入适配 (textarea placeholder "例: 我昨天去了学校")
  - mock 翻译 fallback (5 个常用句: "我昨天去了学校" → "I went to school yesterday.")
  - 不破坏 v1.6 修复 (切 tab useEffect + 文本变量截断)
- **测试**: 6 单元测试 (parseTranslation JSON / mock 翻译 / markdown fence / 解析失败 / cached / fields)

### P2: v1.10.0-B 同义词辨析 (B6, 1d → 0.5d 干)
- **Why**: "happy/glad/joyful/cheerful" 区别? 用户写作用错 → LLM 帮你辨析
- **Scope**:
  - `src/lib/synonyms.ts` (~80 行):
    - `getSynonyms(word, pos)`: 返回 {synonyms: [{word, nuance, example}], confused: [{word, diff}]}
    - 复用 getOrCreateExplanation 缓存 (`synonym::${word}::${pos}`)
    - `synonymKey`: 缓存 key
    - `parseSynonyms`: 解析 LLM JSON 响应
    - `mockSynonyms`: 8 词 fallback (happy/glad/sad/angry/big/small/good/bad)
  - `src/components/SynonymsButton.tsx` (~150 行): 复用 UsageButton 模式
  - WordDetail 加 "🔀 同义词" 按钮 (在 GrammarButton 之后)
- **测试**: 5 单元测试 (parseSynonyms 有效 / markdown fence / mock 8 词 / cached / synonymKey)

### P3: v1.10.0-C 例句 TTS 跟读 (B7, 1d → 0.5d 干)
- **Why**: 单词跟读 (C8 v1.8.0) 升级 — 例句也要跟读
- **Scope**:
  - WordDetail 例句区: 每个例句加 "🎤 跟读" 按钮 (复用 PronunciationPractice + ex.en)
  - 复用 C8 弹窗模式 (showPronounce + state)
  - 但**改成例句级别**: state 用 `pronounceText: string` (可动态设置)
  - tests/wordDetailExampleTTS.test.ts (3 测试): WordDetail 引用 PronunciationPractice + 例句有 TTSButton 旁跟读
- **WONTFIX**: 例句 TTS 自动连续播放 (复杂, v1.11+)

## 共享约束 (3 producer 都要遵守)

- ❌ 不动 v1.6 review 修复的 13 处
- ❌ 不动 v1.7 完结的 3 个新功能 (listeningRecommend / GrammarButton / e2eTest)
- ❌ 不动 v1.8+v1.9 完结的 (Onboarding / 难度自适应 / Daily 100 / OpenRouter / WordDetail 跟读)
- ❌ 不重写 WritePage / llmTutor / aiChat 协议
- ❌ 不引付费依赖 / npm install
- ✅ 0 P0 + 0 P1 + 0 P2 静态审查
- ✅ 用 unknown + Error 守卫 (v1.6 规范)
- ✅ setLoading(true) 修复 (v1.6 规范)

## 验证清单

```bash
cd /workspace/english-app
npx tsc --noEmit                                          # 0 错误
npx vitest run                                            # 164 + 14 = 178 单元测试全过
npx vite build                                            # pass
node scripts/verify-v1.10.0.mjs                           # 闭环 (新写)
python3 scripts/review-v1.10.0.py                         # 静态审查 (新写)
```

## 提交计划

1. `feat(v1.10.0-A): 中译英 Tab (WritePage 加第二 Tab)`
2. `feat(v1.10.0-B): 同义词辨析 (SynonymsButton + synonyms.ts)`
3. `feat(v1.10.0-C): 例句 TTS 跟读 (WordDetail 例句加跟读)`
4. `docs(v1.10.0): CHANGELOG + DEV_LOG + ROADMAP + Release Notes`
5. tag v1.10.0 + push

预计 1d 干完。

## 北极星对齐

- **触发可业**: 中译英 Tab 是学生/职场刚需, 写中文就能用
- **内容能用**: 同义词辨析帮用户避开"中式英语"陷阱
- **学得会**: 例句跟读强化"读"维度 (单词跟读 → 例句跟读)

## 风险

- R1: 中译英 LLM 翻译质量 → Mock 渠道 fallback (5 句常用)
- R2: 同义词辨析 LLM 输出太长 → 限制 maxTokens 600
- R3: 例句跟读弹窗频繁开关 → state 复用 showPronounce
- R4: subagent 失败 (token rate limit 14+ 次) → 主人接管 + 静态审查四件套
