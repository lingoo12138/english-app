# v1.8.0 + v1.9.0 并行开发 (3 producer 1d 干完 6d 计划)

**日期**: 2026-07-24
**作者**: Mavis
**基线**: v1.7.0 (B 听力自适应 + LLM Tutor 2.0 + LLM e2e)
**目标**: 留存修复 + AI 对话扩展 + 真实 LLM + 顺手 2 个小优化

## 团队讨论产物 (3 文档)
- next-phase-plan-v2.md: 4 周 v1.8-v1.11 推荐计划 (W9 onboarding / W10 难度自适应 / W11 中译英+同义词 / W12 FSRS+日报)
- next-phase-plan.md: 旧 v0.23-v0.26 计划 (已完成)
- plan-review.md: 没拿到 (404)

## 3 个并行 Producer

### P1: v1.8.0-A 首启 onboarding (W9) — 留存修复
- **Why**: 当前最大留存漏洞 (C1), 新用户进 Home 看到一堆卡不知道点啥
- **Scope**:
  - `src/components/Onboarding.tsx` (~200 行): 3 步 (1️⃣ 选学段 A1/B1/C1 2️⃣ 体验跟读随机 1 词 3️⃣ 加生词本 + 跳首页)
  - `localStorage.onboarded=true` 标记, 只显示一次
  - 可跳过 (右上角 ✕)
  - `src/pages/Home.tsx` 改造: onboarded=false 时显示 "👋 第一次来? 跟我 5 分钟了解" CTA
  - `Settings.tsx` 加 "🔄 重新看引导" 按钮
  - a11y: focus trap + Esc 退出
- **测试**: 6 单元测试 (Onboarding state 切换 + localStorage 标志 + skip)
- **WONTFIX**: 动画/视频介绍, A/B 测试, 数据回传

### P2: v1.9.0 难度自适应 + 自由话题 (W10) — AI 对话扩展
- **Why**: 隐性升级产品感强 + "为什么我的 AI 对话只限制 5 场景?" 的自然扩展
- **Scope**:
  - `src/lib/aiChat.ts` 改造:
    - `assessUserLevel(messages): 'A1'|'A2'|'B1'|'B2'|'C1'|'C2'` 基于最近 5 轮 user 消息平均词数和句法复杂度
    - `buildSystemPrompt` 加 dynamic level
    - `chat(ctx)` 加 `customTopic` 字段
  - `src/pages/AIChat.tsx` 改造:
    - 难度选择器下方加 "✨ 自动" 切换
    - 顶部场景 chips 末尾加 "💬 自由话题" 按钮 → 弹 modal 输入 topic
  - 单元测试 +6 (`tests/aiChat.test.ts` 已有 3, 加 6 = 9)
- **WONTFIX**: LLM-based 自适应 (本地简单算法足够), 难度持久化, 多角色对话

### P3: OpenRouter 真实 LLM 配 + C4 + C8 — 顺手小优化
- **Why**: 0 成本接 OpenRouter free (gemini-2.5-flash:free) 真 e2e 验证; C4 Daily 100 句扩数据; C8 WordDetail 加跟读按钮
- **Scope**:
  - **OpenRouter free**:
    - `src/lib/providers/llm.ts` 加 OpenRouter free 渠道 (已有, 验证配置)
    - `src/lib/providers/llm.ts` 修正 defaultModel: 'google/gemini-2.5-flash:free'
    - `src/pages/Settings.tsx` LLM 渠道卡片加 "🆓 0 成本" 标签
    - 真实 e2e: 配置 OpenRouter key 后, v1.8-B e2eTest 跑通真实请求
  - **C4 Daily 100 句**:
    - `public/data/daily.json` 30 句 → 100 句 (用现有 30 句 + 新生成 70 句)
    - 复用 AIChat 模式, 不重写 daily.ts
  - **C8 WordDetail 跟读按钮**:
    - `src/pages/WordDetail.tsx` 加 "🎤 跟读" 按钮
    - 复用 PronunciationPractice 组件 + word.word 作为 text
  - 单元测试 +4 (OpenRouter config + Daily 100 句循环 + WordDetail 跟读入口)
- **WONTFIX**: 真实 LLM 端到端 (用户没配 key, mock 渠道够用, 但配 OpenRouter free 是 0 成本)

## 共享约束 (3 producer 都要遵守)

- ❌ 不动 v1.6 review 修复的 13 处
- ❌ 不动 v1.7 完结的 3 个新功能 (listeningRecommend / GrammarButton / e2eTest)
- ❌ 不重写已有 aiChat/llmTutor/providers/llm/daily 协议
- ❌ 不引付费依赖 / npm install
- ✅ 0 P0 + 0 P1 + 0 P2 静态审查
- ✅ 用 unknown + Error 守卫 (v1.6 规范)
- ✅ setLoading(true) 修复 (v1.6 规范)

## 验证清单

```bash
cd /workspace/english-app
npx tsc --noEmit                                          # 0 错误
npx vitest run                                            # 130+ 单元测试全过
npx vite build                                            # pass
node scripts/verify-v1.8.0.mjs                            # 闭环 (新写)
python3 scripts/review-v1.8.0.py                          # 静态审查 (新写)
```

## 提交计划 (3 producer → 3 commits → 1 tag v1.8.0)

1. `feat(v1.8.0-A): 首启 onboarding 3 步引导`
2. `feat(v1.9.0): 难度自适应 + 自由话题`
3. `feat(v1.8.0-C): OpenRouter 真实 LLM 配 + Daily 100 句 + WordDetail 跟读`
4. `docs(v1.8.0): CHANGELOG + DEV_LOG + ROADMAP + Release Notes`
5. tag v1.8.0 + push

预计 1d 干完。

## 北极星对齐

- **触发可业**: onboarding 拉新→激活关键路径
- **内容能用**: 难度自适应让用户挑战合适难度, 自由话题扩展场景
- **学得会**: 跟读入口强化"听"维度, Daily 100 句延长使用周期

## 风险

- R1: onboarding 太长用户跳过 → 严格 3 步 + ≤ 2 分钟
- R2: 难度判定不准 → 至少 3 轮才评估, 不只看长度
- R3: subagent 失败 (token rate limit 12+ 次) → 主人接管 + 静态审查四件套
